spring:
  mail:
    properties:
#      "[mail.smtp.connectiontimeout]": 5000
#      "[mail.smtp.timeout]": 3000
#      "[mail.smtp.writetimeout]": 5000
      host: smtp.gmail.com
      port: 587
      username: ${username}
      password: ${password}
      mail:
        smtp:
          auth: true
        starttls:
          enable: true