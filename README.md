# demo project for javamail in spring boot

Testado apenas para conexão com gmail.
variáveis de ambiente que devem ser passadas para que a aplicação:

```shell
username=<mail>@gmail.com
password=<secret>
```